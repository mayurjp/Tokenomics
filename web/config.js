// Public config. Nothing secret goes here — this file is served to every visitor.
// Point this at your deployed Worker; for local dev, `wrangler dev` serves on :8787.
window.API_BASE = "http://localhost:8787";
